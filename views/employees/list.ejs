<!DOCTYPE html>
<html>

<head>
    <title>Crud sample using PostgreSQL and NodeJs - <%=title%></title>
    <link rel="stylesheet" href="../css/style.css">
</head>

<div class="page-header">
    <h2><%=title%></h2>
</div>
<div class="page-data">
    <div class="data-btn">
        <a href="/employee/add">Add Employee</a>
    </div>
    <div class="data-table">
        <table class="center">
            <tr>
                <th width="50px">No</th>
                <th>Name</th>
                <th>Email</th>
                <th>Address</th>
                <th>Phone</th>
                <th width="120px">Action</th>
            </tr>

            <% if (data.length) {
            for(var i = 0;i < data.length;i++) { %>
            <tr>
                <td><%=(i+1)%></td>
                <td><%=data[i].name%></td>
                <td><%=data[i].email%></td>
                <td><%=data[i].address%></td>
                <td><%=data[i].phone%></td>
                <td>
                    <a class="a-inside edit" href="/employees/<%=data[i].id%>">View</a>
                    <a class="a-inside edit" href="/employees/<%=data[i].id%>/edit">Edit</a>
                    <a class="a-inside delete" href="../employees/<%=data[i].id%>/delete">Delete</a>
                </td>
            </tr>
            <% }
            } else{ %>
            <tr>
                <td colspan="6">No Employees</td>
            </tr>
            <% } %>
        </table>
    </div>
</div>
</body>

</html>